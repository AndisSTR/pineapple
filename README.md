# pineapple

to make full use of the web application, create database "pineaple"
and create table user_emails

CREATE TABLE users_emails(
id INT AUTO_INCREMENT,
   email VARCHAR(50),
   register_date DATETIME,
   PRIMARY KEY(id)
);
